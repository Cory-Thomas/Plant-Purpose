const initialState = {
  date: '',
  id: '',
  image_url: '',
  nickname: '',
  species: '',
  user_id: '',
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
